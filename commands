# 1) Untrack a single file (example: package-lock.json)
git rm --cached package-lock.json
git commit -m "Stop tracking package-lock.json"
git push

# 2) Untrack a folder (example: node_modules)
git rm -r --cached node_modules
git commit -m "Stop tracking node_modules"
git push

# 3) Untrack all files now covered by .gitignore (safe general approach)
git rm -r --cached .
git add .
git commit -m "Refresh tracked files to respect .gitignore"
git push

# 4) Verify
git status
git ls-files --others --ignored --exclude-standard
